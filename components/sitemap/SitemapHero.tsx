import { Heading, VStack } from '@chakra-ui/react';

export const SitemapHero = () => {
  return (
    <VStack align={'stretch'} spacing={8}>
      <Heading as={'h1'} fontSize={'h1'} py={8}>
        خريطة الموقع
      </Heading>
    </VStack>
  );
};
