import { directory as post } from './post';
import { directory as taxonomy } from './taxonomy';
import { directory as user } from './user';

export const getDataDirectory = {
  post,
  taxonomy,
  user,
};
